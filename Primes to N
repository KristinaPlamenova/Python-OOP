num = int(input())
numbers = []

for i in range(1, num + 1):
    is_prime = True

    for div in range(2, i):
        if i % div == 0:
            is_prime = False
            break



    if is_prime:
        numbers.append(i)

print(*numbers)

word = input()

current = word[0]
longest = word[0]

for i in range(1, len(word)):
    if word[i] == word[i - 1]:
        current += word[i]
    else:
        current = word[i]

    if len(current) > len(longest):
        longest = current

print(longest)
